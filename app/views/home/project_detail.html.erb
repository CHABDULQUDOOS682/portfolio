<div class="main-content">
  <div class="top-nav">
    <h3 class="heading heading-sec section-heading">Project Details</h3>
  </div>

  <div class="dashboard-content">
    <% if notice.present? %>
      <p class="notice"><%= notice %></p>
    <% end %>

    <div class="projects-list__project">
      <h3 class="projects-list__heading">
        <%= @project.title %>
      </h3>

      <!-- Image Slider -->
      <div class="slider-container">
        <button class="slider-btn prev-btn">&#10094;</button>

        <div class="slider">
          <% if @project.images.attached? %>
            <% @project.images.each_with_index do |image, index| %>
              <div class="slide <%= 'active' if index == 0 %>">
                <%= image_tag image, class: "projects-list__img" %>
              </div>
            <% end %>
          <% else %>
            <div class="slide active">
              <p class="no-image">No Image Available</p>
            </div>
          <% end %>
        </div>

        <button class="slider-btn next-btn">&#10095;</button>
      </div>

      <!--      <div class="projects-list__actions">-->
      <%#= link_to "#{root_path}#MyProjects", class: "btn-icon btn-view" do %>
      <!--          🔙 Back-->
      <%# end %>
      <!--      </div>-->
    </div>
    <div class="projects-list__project">
      <p class="text-tertiary projects-list__text-tertiary">
        <%#= @project.description %>
        If your page is scrolling to the wrong part of the section (such as the center or bottom instead of the top),
        you can use JavaScript to ensure it scrolls precisely to the top of each section. Here's an improved version
        using scrollIntoView() to control the scrolling behavior: If your page is scrolling to the wrong part of the
        section (such as the center or bottom instead of the top), you can use JavaScript to ensure it scrolls precisely
        to the top of each section. Here's an improved version using scrollIntoView() to control the scrolling behavior:
      </p>
    </div>
    <div class="projects-list__project">
      <p class="text-tertiary projects-list__text-tertiary">
        <%= @project.description %>
      </p>
    </div>
    <div class="projects-list__project">
      <p class="text-tertiary projects-list__text-tertiary">
        <%= @project.description %>
      </p>
    </div>
  </div>
</div>

<script>

</script>
