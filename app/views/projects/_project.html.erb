<div id="<%= dom_id project %>">
  <p>
    <strong>Title:</strong>
    <%= project.title %>
  </p>

  <p>
    <strong>Description:</strong>
    <%= project.description %>
  </p>

  <h3>Images:</h3>
  <% project.images.each do |image| %>
    <%= image_tag image, width: 200 %>
  <% end %>

</div>
